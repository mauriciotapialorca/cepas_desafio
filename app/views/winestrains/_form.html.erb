<%= form_with(model: winestrain) do |form| %>
  <% if winestrain.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(winestrain.errors.count, "error") %> prohibited this winestrain from being saved:</h2>

      <ul>
        <% winestrain.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :porcentaje, style: "display: block" %>
    <%= form.number_field :porcentaje %>
  </div>

  <div>
    <%= form.label :wine_id, style: "display: block" %>
    <%= form.text_field :wine_id %>
  </div>

  <div>
    <%= form.label :strain_id, style: "display: block" %>
    <%= form.text_field :strain_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
